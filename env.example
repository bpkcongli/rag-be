# Copy to .env (outside this workspace if needed) and fill with your local values.
APP_NAME=rag-backend
APP_ENV=local

# MySQL
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=password
MYSQL_DATABASE=rag_pipeline

# Storage
STORAGE_DIR=/Python/rag-pipeline/storage
